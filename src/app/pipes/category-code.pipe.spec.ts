import { CategoryCodePipe } from './category-code.pipe';

describe('CategoryCodePipe', () => {
  it('create an instance', () => {
    const pipe = new CategoryCodePipe();
    expect(pipe).toBeTruthy();
  });
});
